"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
be you """
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
sayalama 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usadvdhz 
 behdbeudbeu
  hdz
   """
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
 nolomokino 
 """
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
kiro check_same_thread
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
holololo 
"""
Script pour initialiser la base de données backend
"""
import sys
sys.path.insert(0, 'backend')

from backend.database import init_database

print("🔧 Initialisation de la base de données backend...")
init_database()
print("✅ Base de données backend initialisée avec succès!")
based in usa
